# yt_thumbnails

Написать сервис по получению превью с youtube. Это должен быть grpc сервис
Написать консольную команду для похода в сервис выше

Команды для второго сервиса
- async: Используется для многопоточного скачивания превью.
- file: string: файл со ссылками на видео. Не может быть использован вместе с флагом 'v'. (не реализовано)
- out: string: Путь по которому скачаются превью. (по умолчанию в текущую папку)
- t: Показывает время получения всех запрошенных превью. При повторном запуске показывает, что кеширование работает. (не реализовано)
- v: string: Ссылки на видео. (не реализовано)


Сервис по получению превью должен уметь их кешировать и отдавать из кэша (не реализовано)
